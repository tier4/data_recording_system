---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: restart drs-sensor-launch
  ansible.builtin.systemd:
    name: drs-sensor-launch.service
    state: restarted
  become: true
