---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: restart drs-record-launch
  ansible.builtin.systemd:
    name: drs-record-launch.service
    state: restarted
  become: true