---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: restart drs-recorder-launch
  ansible.builtin.systemd:
    name: drs-recorder-launch.service
    state: restarted
  become: true