<launch>
  <arg name="vehicle_id" default="default"/>
  <arg name="live_sensor" default="True"/>

  <!-- trigger -->
  <!-- camera0 -->
  <!-- camera1 -->
  <!-- camera2 -->
  <!-- camera3 -->
  <!-- front LiDAR -->
  <!-- right LiDAR -->
  <!-- CAN -->
  <!-- vehicle interface -->
  <group if="$(var live_sensor)">
    <include file="$(var vehicle_launch_pkg)/launch/can_interface.launch.xml">
      <arg name="vehicle_id" value="$(var vehicle_id)"/>
    </include>
  </group>

  <!-- topic monitor -->
  <!-- Driver Interface -->
</launch>
