/**:
  ros__parameters:
    # rectification implementation
    rect_impl: npp
    #  Flag whether OpenCV is used to generate rectification mapping
    use_opencv_map_init: false
    # Alpha value for rectification
    alpha: 0.0
    # Jpeg quality
    jpeg_quality: 80
    # Do not perform rectifiaction
    do_rectify: false
